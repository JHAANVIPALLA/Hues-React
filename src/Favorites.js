import React from 'react';
import './styles/Favorites.css';  // Include the CSS for styling

const Favorites = () => {
  return (
      <div className="Favorites">
        <h2>No favorite items yet. how about adding some?</h2>
      </div>

  );
};

export default Favorites;
