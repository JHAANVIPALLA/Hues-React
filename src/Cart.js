import React from 'react';
import './styles/Cart.css';  // Include the CSS for styling

const Cart = () => {
  return (
      <div className="Cart">
        <h2>Your cart is empty!</h2>
      </div>

  );
};

export default Cart;
